{
  "name": "agenthub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "db:generate": "cd apps/web && drizzle-kit generate",
    "db:migrate": "cd apps/web && drizzle-kit migrate",
    "db:push": "cd apps/web && drizzle-kit push"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.0.0"
}
