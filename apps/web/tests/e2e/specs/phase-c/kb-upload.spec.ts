import { test, expect } from "@playwright/test";

test.describe("Knowledge Base Upload", () => {
  test("user uploads PDF to knowledge base", async ({ page }) => {
    // TODO: Implement after Phase C
    test.skip(true, "Phase C not started");
  });

  test("document appears in KB after chunking", async ({ page }) => {
    test.skip(true, "Phase C not started");
  });
});
