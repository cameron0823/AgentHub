# AgentHub Desktop Resources

`resources/web` is generated by `pnpm -C apps/desktop prepare:web`.

The generated folder contains the Next.js standalone server, static assets, public assets, and `bundle-manifest.json`. Secrets, local service data, and environment files must not be copied into this folder.
